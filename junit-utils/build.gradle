dependencies {
    implementation("junit:junit") {
        version {
            strictly "4.11"
        }
    }
    testImplementation("junit:junit") {
        version {
            strictly "4.11"
        }
    }
}
