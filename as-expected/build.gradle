dependencies {
    gradleLint.ignore("unused-exclude-by-dep") {
        testImplementation(group: "junit", name:"junit", version:"${junitVersion}") {
            exclude module: "hamcrest-core"
        }
    }
}
